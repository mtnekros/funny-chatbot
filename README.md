# General LLM


## Design Pattern:
* My features:
    * A UI to chat with the ChatBot
    * Store chat history
* Components:
    * LLMChain
        * Set system messages
        * Invoke the model
    * UI/Store (Streamlit):
        * Store messages
        * display the msgs.
    * MainLoop

